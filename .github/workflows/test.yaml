on:
  repository_dispatch:
    types: [test]
jobs: 
    simple-java-app:
      runs-on: ubuntu-latest
      steps:
        - name: checkout
          uses: actions/checkout@v6.0.2
        - name: Build the app
          run: mvn clean package
          shell: bash  
                
