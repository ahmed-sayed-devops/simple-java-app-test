on:
  push:
    branches:
      - "main"
jobs: 
    simple-java-app:
      runs-on: ubuntu-latest
      steps:
        - name: checkout
          uses: actions/checkout@v6.0.2
        - name: Build the app
          run: mvn clean package
          shell: bash  
                
